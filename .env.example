# 网络配置API服务设置

# 监听地址 (默认: 0.0.0.0 表示监听所有接口)
NETWORK_CONFIG_HOST=0.0.0.0

# 监听端口 (默认: 8080)
NETWORK_CONFIG_PORT=8080

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# 是否启用调试模式 (true/false)
DEBUG=false

# API前缀
API_PREFIX=/api/v1

# 注意: 
# 1. 将此文件复制为.env文件进行实际配置
# 2. 不要将.env文件提交到版本控制
# 3. 需要重启服务使配置生效